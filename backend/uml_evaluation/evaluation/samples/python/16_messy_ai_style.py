# SAMPLE 16 — python_messy_ai_style
# COMPLEXITY: medium
# SIMULATES: Python code generated by AI with inconsistent conventions —
#             mixed snake_case and camelCase, missing type hints on some fields,
#             redundant self assignments, inconsistent method naming
# CHALLENGE: field extractor must handle missing annotations and mixed styles
# EXPECTED: 3 classes, 9 fields, 10 methods, 2 relationships

class studentInfo:   # AI ignored PEP8 class naming
    def __init__(self, id, name, gpa):   # missing type hints — common AI output
        self.id = id
        self.studentName = name    # inconsistent: should be student_name
        self.GPA = gpa             # inconsistent capitalization
        self.GPA = self.GPA        # AI sometimes generates redundant assignments

    def getId(self):   # camelCase instead of get_id — AI mixing Java habits
        return self.id

    def getStudentName(self):
        return self.studentName

    def updateGPA(self, new_gpa):   # mixed naming convention
        self.GPA = new_gpa

    def get_gpa(self):   # now snake_case — AI inconsistency
        return self.GPA


class CourseRecord:
    def __init__(self, course_id: str, title: str):
        self.course_id: str = course_id
        self.title: str = title
        self.enrolled_student = None   # missing type hint, singular not List

    def assign_student(self, student):   # no type hint on param
        self.enrolled_student = student

    def get_course_id(self) -> str:
        return self.course_id

    def get_title(self) -> str:
        return self.title


class progressTracker:   # AI ignored PEP8 again
    def __init__(self):
        self.records = []   # no type hint — AI omission
        self.current_student: studentInfo = None

    def add_record(self, record: CourseRecord) -> None:
        self.records.append(record)

    def get_records(self):   # missing return type
        return self.records

    def get_progress_summary(self) -> str:
        return f"Records: {len(self.records)}"